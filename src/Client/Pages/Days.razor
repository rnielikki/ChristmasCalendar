@page "/day/{Day:int}"
@inject AdventCalendar.Services.Validator validator
@inject AdventCalendar.Services.DayReader reader
@inject IDateTime date

<div class="wrapper">
    <h1>Christmas calendar</h1>
    <div class="day-wrapper">
        <DaysContent Year="date.Year" Day="Day">
            <h2 class="day-header">@context.Title</h2>
            <div class="day-content">
                @((MarkupString)context.Content)
                <br />
                <NavLink class="linkbtn" href="/">Back to the main</NavLink>
            </div>
        </DaysContent>
    </div>
</div>
@code{
    [Parameter]
    public int Day { get; set; }
}
